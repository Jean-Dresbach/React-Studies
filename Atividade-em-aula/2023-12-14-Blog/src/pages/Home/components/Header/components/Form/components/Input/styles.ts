import styled from "styled-components"

export const Wrapper = styled.input`
  width: 100%;
  padding: 10px;
  border: none;
  margin-bottom: 10px;
  outline: none;
  color: ${({ theme }) => theme.colors.text};
`
